info:
  name: me
  type: http
  seq: 4

http:
  method: GET
  url: "{{authUrl}}/me"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
